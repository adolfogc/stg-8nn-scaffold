##
## bsp
##

set(BSP_LIBRARY bsp)
set(BSP_DIR bsp)

list(APPEND BSP_SOURCES
  ${BSP_DIR}/bsp.c
  ${BSP_DIR}/bsp_qpc.c
  ${BSP_DIR}/bsp.h
)

add_library(${BSP_LIBRARY} STATIC ${BSP_SOURCES})
target_include_directories(${BSP_LIBRARY} PRIVATE ${BSP_DIR})
target_include_directories(${BSP_LIBRARY} PRIVATE ${QPC_INCLUDE})
target_include_directories(${BSP_LIBRARY} PRIVATE ${LIBOPENCM3_DIR}/include)
